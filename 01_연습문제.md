# 1

* Q: 파이썬 기반의 딥러닝 개발 환경을 구성할 때 가상 환경을 사용하는 이유는?

* A: 가상 환경을 사용하지 않으면 개별 프로젝트가 서로 다른 python이나 패키지 버전을 사용할 때 충돌이 발생할 수 있고 패키지 제거 시 전체 시스템이 망가질 수 있기 때문이다. 또한 가상 환경을 사용하면 복잡한 의존성을 쉽게 프로젝트별로 독립적으로 관리할 수 있다.


# 2

* Q: 파이토치 텐서가 넘파이 배열과 유사한 형태를 가지면서도 딥러닝 학습에 더욱 적합한 이유는?

* A: 여러 가지 이유가 존재한다.

  1. CPU 연산만 지원하는 `numpy`와 달리, `torch.tensor`는 GPU나 TPU 병렬 연산도 지원한다.

  2. `torch.tensor`는 `requires_grad` 필드를 이용한 `torch.backward()` 미분 연산을 지원한다.


# 3

* Q: 텐서의 형태와 차원 구조를 올바르게 이해하는 것이 중요한 이유

* A: 텐서 형태 차이로 발생할 수 있는 오류를 예방하고, 연산 구조를 논리적으로 유지할 수 있기 때문이다.


# 4

* Q: 두 개의 numpy 배열 A( shape: (2, 3)), B(shape: (3, )) 에서 A + B의 연산이 가능한 이유는?

* A: numpy.array에서는 일반 list와는 달리 Broadcasting 연산이 이루어지기 때문이다. 이는 Pytorch에서도 동일하다.
* 차원 수가 상대적으로 낮은 배열의 왼쪽에 1을 자동으로 채워 차원의 개수를 일치시킨다.
* A(2, 3), B(1, 3)이 되므로 첫 번째 차원은 어느 한 쪽이 1이고, 두 번째 차원은 같다.
* 따라서 B를 확장하여 연산을 수행할 수 있다.